![](assets/1.3%20定义新的函数/file-20260112162602584.png)

定义了一个叫 `square` 的新函数， `x` 叫做形式参数（形参）

如何定义函数：
1. `def` 语句：指明函数的名称和一个以逗号分隔的形式参数列表
2. `return` 语句（函数体），写的是返回的表达式


```python
def <函数名>(<形式参数列表>):
	return <返回表达式>
```

ha